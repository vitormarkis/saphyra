---
title: onConstruct
description: A function that runs before the store is instantiated. Returns the first state of the store + derivations.
---

### How does it looks like:
```js
const newAuthStore = newStoreDef({
  onConstruct({ initialProps }) {
    const [name, surname] = initialProps.name.split(" ")
    
    return {
      name,
      surname,
    }
  },
})


